{{- if .Values.observation.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "HyFive.fullname" . }}
  labels:
    {{- include "HyFive.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "HyFive.fullname" . }}
  minReplicas: {{ .Values.observation.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.observation.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.observation.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.observation.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.observation.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.observation.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
