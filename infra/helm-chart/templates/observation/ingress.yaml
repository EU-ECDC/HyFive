{{- if .Values.observation.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "HyFive.fullname" . }}
  labels:
    {{- include "HyFive.labels" . | nindent 4 }}
  {{- with .Values.observation.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.observation.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.observation.ingress.tls }}
  tls:
    {{- range .Values.observation.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.observation.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "HyFive.fullname" $ }}
                port:
                  number: {{ $.Values.observation.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
